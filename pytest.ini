[pytest]
markers =
    sqlite: Tests that use SQLite graph store
    neo4j: Tests that use Neo4j graph store (requires Neo4j running)
    slow: Tests that take longer to run
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

